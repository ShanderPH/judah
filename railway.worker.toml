# Railway deployment configuration for Celery Worker
# https://docs.railway.app/reference/config-as-code

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.worker"

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
